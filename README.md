# Instrucciones

En la carpeta `src/main/resources` está el archivo `gestion_proyectos.sql` que crea la base de datos de gestión de proyectos y carga datos iniciales para departamentos, empleados y tareas.
